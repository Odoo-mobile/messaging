<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:odoo="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:paddingTop="?android:attr/actionBarSize" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" >

        <include
            android:id="@+id/loadingProgress"
            layout="@layout/listview_data_loading_progress"
            android:paddingBottom="5dp"
            android:paddingTop="5dp"
            android:visibility="gone" />

        <android.widgets.SwipeRefreshLayout
            android:id="@+id/swipe_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >

            <odoo.controls.OList
                android:id="@+id/lstMeesages"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                odoo:custom_layout="@layout/mail_list_item"
                odoo:emptyListIcon="@drawable/ic_action_inbox" >
            </odoo.controls.OList>
        </android.widgets.SwipeRefreshLayout>
    </LinearLayout>

    <odoo.controls.fab.FloatingActionButton
        android:id="@+id/fabbutton"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_gravity="bottom|right"
        android:layout_marginBottom="16dp"
        android:layout_marginRight="16dp"
        android:onClick="fabClicked"
        odoo:drawable="@drawable/ic_action_user"
        odoo:fabColor="#FFDF4D38" />

</FrameLayout>